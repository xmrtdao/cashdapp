The web_fetch command successfully retrieves data; however, the 'edit' command is unstable and will often timeout or not save.

playwright browser: Chrome extension relay is running, but no tab is connected.  The user must click the OpenClaw Chrome extension icon on a tab to attach it (profile "chrome").

The skills are being blocked due to unrecoverable errors from core tools.