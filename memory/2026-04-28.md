# 2026-04-28

## Heartbeat Cycle 1 (08:45 CST)

### Task Runner / Cron Fix
- Root cause: `openclaw gateway restart` fails on Windows (SIGUSR1/schtasks errors), leaving cron child sessions orphaned
- 13 orphaned tasks cleared by deleting and recreating the 3 cron jobs:
  - `task-digest` (every 4h, 4a00b50e)
  - `morning-briefing` (8am, 3209ffd9)
  - `wa-check-in` (5pm, 5681db3d)
- All 3 crons now in **idle** state (healthy)
- Old orphaned entries reference deleted cron IDs; they'll age out

### Memory Embeddings Fix
- Google Gemini 403 PERMISSION_DENIED blocking memory_search
- Ollama embeddings: needs 16.8GB RAM (only 6.6GB available)
- DeepSeek: no embeddings endpoint
- **Fix applied**: `agents.defaults.memorySearch.store.vector.enabled = false` → FTS-only mode
- Gateway needs restart to fully apply (config change written to openclaw.json)

### WhatsApp Gateway
- Reconnected at +50661500559 (12:15 CST)
- Note: `npx openclaw message send` SIGKILLs on this machine (exec timeout issue, not a code problem)

### Git
- Committed 15 files: heartbeat sync, memory fix, cron rebuild, MCP tool updates
- Submodules (MESHNET, XMRT-DAO-Ecosystem, moltmall, xmrtnet_repo) still have uncommitted local changes

### Known Issues
- Gateway restart still needed to apply vector embeddings disable
- `npx openclaw message send` SIGKILLs - investigate exec timeout root cause
- 14 orphaned task entries still in task list but referencing deleted cron IDs
- Memory search 403 (resolved by FTS-only mode)
