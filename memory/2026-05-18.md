# 2026-05-18 — Full Infrastructure Day: Tunnel, Dashboard, Campaigns, Fleet Comms, Email Replies

## Session: Full Day (Joe + Vex)

### System State (End of Day)
- **Relay:** v5.0.0, 47 tools, 7 handlers, stable
- **Tunnel:** relay.mobilemonero.com — permanent named tunnel (xmrtsolutions Cloudflare account)
- **Ollama:** 13 models ✅
- **Supabase:** OK ✅
- **Cloudflare workers:** fleet.mobilemonero.com ✅ ai.mobilemonero.com ✅ api.mobilemonero.com ✅

### Tunnel Fix
- Tunnel was down (1033 error) → restarted cloudflared as independent process
- Webhook URLs updated for both Resend accounts (PFP + XMRT)
- Tunnel URL persisted to relay state

### Dashboard Fix — Dynamic Data Wasn't Loading
Root cause: `type: () => true` on express.json() was breaking GET endpoint parsing. Reverted to default.
Also: dead JS functions (`loadMiningStats`, `selfReportLocalMiner`, `loadWebPoolStats`) were referencing removed HTML IDs. Cleaned up.
Also: tunnel had 502 after relay restart — needed tunnel restart too.

**Before/After:**
- `/api/fleet`: 5.2s → 0.28s (fixed dead Hermes tunnel fetch)
- `/resend/inbox`: 3.7s / 208KB → 0.26s / 7.7KB (via /brief endpoint)
- Dashboard: all cards now render, JS executes cleanly

### Campaign Fix — Duplicate Emails & Extra Tasks
**Root cause:** 7 campaign tasks were running daily (8AM, 12PM, 2PM, 4PM, 6PM, 8PM, 10PM each sending 20-50 emails). Falls Church PTSA (Courtnie Ross) reported getting 5 emails.
**Fix:**
- Disabled 4 extra tasks (2PM, 6PM, 8PM, 10PM)
- Added file-based lock to prevent concurrent campaign runs
- Incremental sent-email logging (each email saved immediately, not batched)
- Added defense-in-depth duplicate check
- Remaining tasks: DailyCampaign (8AM/50), NoonCampaign (12PM/50), 4PMCampaign (4PM/50), SeasonalScraper (11PM)
- Noon and 4PM tasks updated to send 50 per drop

### Contact Pool & Scraping
- Pool grew from 370 to **597 contacts**
- Fresh contacts: **127** (covers remaining drops + tomorrow)
- Scraped DC + Dallas + Virginia corporate conference contacts
- Email template updated: corporate contacts get headshot + reception double-booking pitch

### Mining Tracking
- Pool sync auto-discovers workers (vex-laptop, joe, xmrt-dao-mobile)
- 90/10 split → updated to equal proportional split per worker
- Added `/mining/heartbeat` endpoint for live hashrate reporting
- Fixed SupportXMR pool API URLs (added www. prefix — non-www 301s)
- Dashboard leaderboard shows: live hashrate · shares · XMRT · online status

### Email Replies Sent
- **DC JazzFest** (hannah@dcjazzfest.org) — Quoted $996/day, then offered $96/day + print revenue model. Awaiting reply.
- **Spring Into Summer Festival** (croughanashley@gmail.com) — Pitched partnership model (free setup + revenue share instead of vendor booth fee). Awaiting reply.

### Mobile Mining Script (mmlauncher)
- Added XMRig API config (port 19090) for fleet tracking
- Unique worker names and worker IDs
- Post-install instructions include fleet dashboard URL + auto-start commands
- Restored proper XMRT ASCII logo (box-drawing was mangled in edits)
- Logo: red (31) + cyan (36) tagline + blue divider on Termux
- Pushed to github.com/xmrtdao/mmlauncher

### Fleet Communication
- **Eliza-Cloud** — Reading fleet chat and responding. Had issues with Content-Type header missing on POST requests. Fixed by her using correct header.
- **Hermes (Joe's phone)** — Relay at 192.168.14.115:9090. 5 Cloudflare workers deployed (fleet, ai, api, price, mtv). Fleet routing updated to use api.mobilemonero.com.
- Fleet chat has 70+ messages, all 3 agents active
- GitHub Issues #22 (email tutorial), #23 (PFP template builder), #25 (fleet chat fix) all addressed

### Social Publishing
- First tweet went out May 18, 12PM CST ✅ ("DAO Economy Article Promotion" via Typefully)
- Published at: x.com/XMRTSolutions/status/2056434691591581954
- Second tweet scheduled: May 19, 12PM CST ("Centralized AI Wins in Court" article)
- Typefully API key updated, auth method corrected to Bearer token (v2 API)
- New relay endpoints: `POST /api/typefully/schedule` + `GET /api/typefully/drafts`

### GitHub Issues Created
- #23: PFP Template Builder tutorial for Eliza
- Commented on #22 (email tutorial), #25 (fleet chat fix), #26 (Hermes endpoints), #27 (PFP branding)

### Hyperspace AGI Discovery
- Found github.com/hyperspaceai/agi (1,824 ★) — decentralized P2P agent network
- Uses libp2p GossipSub (same tech we need for Issue #13)
- Pod system lets devices pool compute (laptop + phone + cloud)
- CLI install: `curl -fsSL https://agents.hyper.space/api/install | bash`

### Key Lessons
1. Cloudflare tunnel needs restart after relay restart (502 otherwise)
2. Typefully API v2 uses `Authorization: Bearer`, NOT `X-API-KEY` header
3. `type: () => true` on express.json() breaks GET endpoint parsing
4. 7 campaign tasks were running concurrently — file lock prevents duplicates
5. Hermes workers are deployed and live at mobilemonero.com subdomains
