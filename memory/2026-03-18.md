Potential solution for the "rambling" issue:

1.  Modify the `send` action in the `openclaw-relay` Supabase edge function to include a unique `message_id` in the `metadata`.
2.  OpenClaw checks for this `message_id` when processing inbound messages to avoid processing duplicates.
3.  Configure Eliza to only use one method to send messages to OpenClaw (either `openclaw-relay` or Gemini API, but not both).

Web search is failing.
